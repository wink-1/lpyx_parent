<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wink.lpyx.manager.mapper.product.ProductSkuMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into product_sku (id,
        sku_code,
        sku_name,
        product_id,
        thumb_img,
        sale_price,
        market_price,
        cost_price,
        stock_num,
        sku_spec,
        weight,
        volume,
        status,
        sale_num,
        create_time,
        update_time,
        is_deleted)
        values
        <foreach collection="productSkuList" item="item" separator=",">
            (#{item.id}, #{item.skuCode}, #{item.skuName}, #{item.productId}, #{item.thumbImg}, #{item.salePrice},
            #{item.marketPrice}, #{item.costPrice}, #{item.stockNum}, #{item.skuSpec}, #{item.weight}, #{item.volume},
            #{item.status}, #{item.saleNum}, now(), now(), 0)
        </foreach>
    </insert>


    <update id="updateById">
        update product_sku set
        <if test="skuCode != null and skuCode != ''">
            sku_code = #{skuCode},
        </if>
        <if test="skuName != null and skuName != ''">
            sku_name = #{skuName},
        </if>
        <if test="productId != null and productId != ''">
            product_id = #{productId},
        </if>
        <if test="thumbImg != null and thumbImg != ''">
            thumb_img = #{thumbImg},
        </if>
        <if test="salePrice != null and salePrice != ''">
            sale_price = #{salePrice},
        </if>
        <if test="marketPrice != null and marketPrice != ''">
            market_price = #{marketPrice},
        </if>
        <if test="costPrice != null and costPrice != ''">
            cost_price = #{costPrice},
        </if>
        <if test="stockNum != null and stockNum != ''">
            stock_num = #{stockNum},
        </if>
        <if test="skuSpec != null and skuSpec != ''">
            sku_spec = #{skuSpec},
        </if>
        <if test="weight != null and weight != ''">
            weight = #{weight},
        </if>
        <if test="volume != null and volume != ''">
            volume = #{volume},
        </if>
        <if test="status != null and status != ''">
            status = #{status},
        </if>
        update_time = now()
        where
        id = #{id}
    </update>


    <delete id="deleteById">
        update product_sku
        set is_deleted = 1
            and update_time = now()
        where id = #{id}
    </delete>


    <select id="findByProductId" resultType="com.wink.lpyx.model.entity.product.ProductSku">
        select *
        from product_sku
        where product_id = #{id}
          and is_deleted = 0
    </select>

</mapper>