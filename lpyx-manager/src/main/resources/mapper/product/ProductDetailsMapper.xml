<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.wink.lpyx.manager.mapper.product.ProductDetailsMapper">

    <insert id="save" useGeneratedKeys="true" keyProperty="id">
        insert into product_details (id,
                                     product_id,
                                     image_urls,
                                     create_time, update_time, is_deleted)
        values (#{id},
                #{productId},
                #{imageUrls},
                now(),
                now(),
                0)
    </insert>

    <update id="updateById">
        update product_details set
        <if test="productId != null and productId != ''">
            product_id = #{productId},
        </if>
        <if test="imageUrls != null and imageUrls != ''">
            image_urls = #{imageUrls},
        </if>
        update_time = now()
        where
        id = #{id}
    </update>


    <delete id="deleteById">
        update product_details
        set is_deleted = 1 and update_time = now()
        where id = #{id}
    </delete>

    <select id="findById" resultType="com.wink.lpyx.model.entity.product.ProductDetails">
        select *
        from product_details
        where id = #{id}
          and is_deleted = 0
    </select>

</mapper>